# Group project
To run it you need to open a terminal in this directory and run:
```sh
python manage.py runserver
```
If it fails, try with `python3`.
You can create a new subproject with
```sh
python manage.py startapp [SUBPROJECT NAME]
```
